@model appWebPrueba.Models.DoctorVM

@{
    Layout = null;
}
<div class="table-responsive">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Doctor</th>
                <th>Nombre</th>
                <th>Estado</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.lGridDoctor != null)
            {
                foreach (var item in Model.lGridDoctor)
                {
                    <tr>
                        <td style="font-size:12px">@item.intDoctor</td>
                        <td style="font-size:12px">@item.strNombreCompleto</td>
                        <td style="font-size:12px">
                            @{
                                string Elementoi = (@item.Estado == true ? "<span title='Activo'><i class='fas fa-check' style='color: green;'></i></span>" : "<span title='Inactivo'><i class='fas fa-times' style='color: red;'></i></span>");
                            }
                            @Html.Raw(@Elementoi)
                        </td>
                        <td style="font-size:12px">
                            <span title="Editar" class='urlEditar' data='@item.intDoctor'><i class='fas fa-edit' style='color: #5d9cec;'></i></span>
                            <span title="Eliminar" class='urlEliminar' data='@item.intDoctor'><i class='fas fa-trash' style='color: red;'></i></span>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>