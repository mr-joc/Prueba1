@model appWebPrueba.Models.ColorVM

@{
    Layout = null;
}
<div class="table-responsive">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Folio</th>
                <th>Color</th>
                <th>Colorimetro</th>
                <th>Estado</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.lGridColor != null)
            {
                foreach (var item in Model.lGridColor)
                {
                    <tr>
                        <td style="font-size:11px">@item.intColor</td>
                        <td style="font-size:11px">@item.strNombre</td>
                        <td style="font-size:11px">@item.strColorimetro</td>

                        <td style="font-size:11px">
                            @{
                                string Elementoi = (@item.Estado == true ? "<span title='Activo'><i class='fas fa-check' style='color: green;'></i></span>" : "<span title='Inactivo'><i class='fas fa-times' style='color: red;'></i></span>");
                            }
                            @Html.Raw(@Elementoi)
                        </td>
                        <td style="font-size:11px">
                            <span title="Editar" class='urlEditar' data='@item.intColor'><i class='fas fa-edit' style='color: #5d9cec;'></i></span>
                            <span title="Eliminar" class='urlEliminar' data='@item.intColor'><i class='fas fa-trash' style='color: red;'></i></span>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>